# Nvim Config
This repo is my neovim config.

## dependencies
- pynvim
- tree-sitter:
    - treesitter-cli:
        - `npm install -g tree-sitter-cli`
- search tools:
    - ripgrep;
    - fd;
    - the_silver_searcher;
